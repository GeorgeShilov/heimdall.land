---
import PostCard from "@/components/PostCard.astro";
import Layout from "@/layouts/Layout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<Layout title="Blog">
  <section class="section">
    <div class="section-inner">
      <p class="eyebrow">Blog</p>
      <h1>Field notes from the decentralized frontier.</h1>
      <p class="lead">
        On-chain analysis, network health signals, protocol upgrades, and practical guides for
        tracking cryptocurrency growth across Bitcoin, Ethereum, and emerging chains.
      </p>
      <div class="post-grid">
        {
          posts.map((entry) => (
            <PostCard
              post={{
                // entry.slug = filename stem = live URL segment (do not use title)
                slug: entry.slug,
                title: entry.data.title,
                description: entry.data.description,
                pubDate: entry.data.pubDate,
                category: entry.data.category,
                tags: entry.data.tags,
                heroImage: entry.data.heroImage,
              }}
            />
          ))
        }
      </div>
    </div>
  </section>
</Layout>

<style>
  .post-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
    margin-top: 42px;
  }

  @media (max-width: 900px) {
    .post-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
